apiVersion: v1
kind: ConfigMap
metadata:
  name: proxy-config
  namespace: jira-k8s-agent
data:
  # Trailing dot on the proxy host = absolute FQDN, skips the ndots:5 search-domain
  # expansion that was failing to resolve the proxy on hldc03 (QA pods do the same).
  HTTP_PROXY: "http://proxy-dmz.intel.com.:912"
  HTTPS_PROXY: "http://proxy-dmz.intel.com.:912"
  NO_PROXY: "10.96.0.1,kubernetes,kubernetes.default,kubernetes.default.svc,kubernetes.default.svc.cluster.local,10.0.0.0/8,habana-labs.com,.habana-labs.com,intel.com,.intel.com,127.0.0.1,localhost,jira-agent,langgraph-agent-service,vllm-service,.svc,.svc.cluster.local"
