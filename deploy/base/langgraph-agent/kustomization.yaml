apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - rbac.yaml
  - rbac-locks.yaml

# ── Image management ──────────────────────────────────────────────────────────
# Change newTag (or newName) here to update the langgraph-agent image.
images:
  - name: jira-triage-langgraph
    newName: artifactory-kfs.habana-labs.com/docker-developers/users/dweinsto/jira-triage-langgraph
    newTag: "latest"
